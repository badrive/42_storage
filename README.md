# 42_storage


 **free-space.sh:** — deletes. Run when you're tight on space. <br>
 **relink-caches.sh:** — relocates future cache growth to `/goinfre`. Run after a `/goinfre` wipe or on a new machine.
